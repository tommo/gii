#ifndef	TMSENSORBEHAVIOR_H
#define	TMSENSORBEHAVIOR_H

class TMSensorBehavior
{
private:
	float mRadius;
	u32   mTeamFlags;

public:
	TMSensorBehavior(arguments);
	~TMSensorBehavior();

	/* data */
};

#endif
